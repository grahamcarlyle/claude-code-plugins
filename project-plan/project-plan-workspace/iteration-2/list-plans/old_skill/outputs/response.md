The `/project-plan` skill supports two operations: **save** and **load**.

The `list` command is not a recognized operation. However, here are the available plans in the `plans/` directory:

| File | Title | Status |
|------|-------|--------|
| `add-auth.md` | Add Authentication | draft |
| `migrate-db.md` | Migrate Database to Postgres | implemented |
| `old-api-redesign.md` | Old API Redesign | rejected |
| `new-api.md` | New API Design | _(none)_ |

Would you like to:
- **load** one of these plans to use as the basis for plan mode? (e.g., `/project-plan load add-auth.md`)
- **save** the current plan to the project plans directory? (e.g., `/project-plan save`)
