**Path rejected.** The path `../../etc/passwd` resolves outside the project root. For security, all plan paths must remain within the project directory. Please provide a path that stays within the project — for example, `plans/my-plan.md` or just a plan name like `my-plan`.

To see available plans, run `/project-plan list`.
